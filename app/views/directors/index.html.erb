<!DOCTYPE html>
<html>
<head>
  <title>Directors</title>

  <style type="text/css">
    th {
      text-align:left;
    }
  </style>
</head>

<body>
  <h1>List of Directors</h1>

  <a href="http://localhost:3000/directors/new_form">Add a New Director</a>

  <table>

    <tr>
      <th width="100">Thumbnail</th>
      <th width="175">Name</th>
      <th width="159">Dob</th>
      <th colspan="3">Actions</th>
    </tr>

    <% @list_of_directors.each do |each_director| %>
    <tr>
      <td>
        <img src="<%= each_director.image_url %>" width="80">
      </td>
      <td width="175">
        <%= each_director.name %>
      </td>
      <td width="150">
        <%= each_director.dob %>
      </td>
      <td>
        <a href="http://localhost:3000/directors/<%= each_director.id %>">Show</a>
      </td>
      <td>
        <a href="http://localhost:3000/directors/<%= each_director.id %>/edit_form">Edit</a>
      </td>
      <td>
        <a href="http://localhost:3000/delete_director/<%= each_director.id %>">Delete</a>
      </td>
    </tr>
    <% end %>
  </table>

</body>
</html>

